# ================= PROMPTHUB DEPENDENCIES =================

# Framework web
Flask==3.1.3

# Server produksi (WSGI). app.run() bawaan Flask tidak disarankan untuk produksi.
# Gunakan: waitress-serve --port=5000 app:app
waitress==3.0.2

# Load .env file
python-dotenv==1.0.1

# MongoDB driver (database online)
pymongo==4.8.0

# Cloudinary SDK (storage media online: gambar & video)
cloudinary==1.41.0

# HTTP client untuk memanggil Gemini API dari server
requests==2.32.3
